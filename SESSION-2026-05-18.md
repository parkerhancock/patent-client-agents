# Session snapshot — 2026-05-18

Built the **Patent Client Index** (working name): the published, opinionated
atlas of patent + IP data sources. Atlas page + map at
`patentclient.com/atlas`, deep-dive synopsis pages at
`docs.patentclient.com/patent-client-index/`. Unified data layer
(`coverage/atlas.json`) feeds both. Cross-links both ways.

This doc captures what landed, the policies that emerged, and what's
queued for the next session.

---

## §1 What landed (by area)

### Data layer (`patent-client-agents`)
- **`coverage/atlas.json`** — new build artifact, office-centric fusion of
  `research/STATE.yaml` + `coverage/sources.yaml`. 46 entities, 9
  unattached sources. Schema documented in
  [`ATLAS_INTEGRATION.md`](ATLAS_INTEGRATION.md) §3.
- **`scripts/build_coverage.py`** — extended with `build_atlas_payload()`,
  `build_atlas_entities()`, `compute_atlas_summary()`, region mapping,
  shipped-source-to-entity prefix matching.
- **`coverage/coverage.json`** — still emitted (back-compat for legacy
  consumers); shape unchanged.
- **`.github/workflows/ci.yml`** — added `coverage-manifest` job that
  runs `build_coverage.py --check` on every PR.

### Atlas viz (`patentclient-web`)
- **`atlas.html`** — new page. Hero strip (title + lede) → big world
  map → legend strip below the map → compact filter bar → card grid.
- **`assets/atlas.js`** (~470 lines, vanilla ESM) — D3 map colored by
  rating, faceted filters with live counts, drawer with verdict basis +
  deep-link, ?focus=ISO2 deep-linking, localhost rewrite for local dev.
- **`assets/atlas.css`** — full styling, light + dark modes.
- **`_redirects`** — `/coverage` → `/atlas` (301 alias).
- **`sitemap.xml`** — added `/atlas` URL.
- **`index.html`** nav → `/atlas`.

### Docs deep dives (`patent-client-agents/docs_hooks/`)
- **`sync_patent_client_index.py`** — mkdocs hook that virtual-mounts
  every `research/{multilateral,regional,national}/*.md` synopsis under
  the `Patent Client Index` tab. Transforms at render time:
  - Frontmatter ` **Label:** value` lines → markdown infobox table
  - `**Label:**` + bullet-list blocks → proper paragraph + `<ul>`
    (FIX-15 & FIX-18 — applies to whole body, not just frontmatter)
  - §5 `Connector strategy` → §5 `Access via patent-client-agents`
  - §6 `Open questions` → §6 `Known unknowns`
  - §8 change log → "Last updated YYYY-MM-DD." footer
  - Cross-refs (`../connectors/X.md`, `../../coverage/sources.yaml`,
    etc.) resolved via `posixpath.normpath` to clean GitHub URLs (no
    literal `..` segments)
  - Verdict callout prepended after frontmatter, with `?focus=` link
    back to the atlas map
- **`docs/stylesheets/extra.css`** — added the infobox-table treatment
  for `article > table:first-of-type`.
- **`mkdocs.yml`** — added the `Patent Client Index` nav tab + the
  hook reference.

### Source synopses (`research/`)
- **Heading set normalized across all 17 synopsis files** (FIX-19):
  ```
  §1 Mission
  §2 What's unique here
  §3 Programmatic surfaces
  §4 Fees                                  ← was "Fee schedule"; canonicalized
  §5 Connector strategy                     ← source name; published as "Access via patent-client-agents"
  §6 Open questions                          ← source name; published as "Known unknowns"
  §7 References
  §8 Change log                              ← collapsed to footer in publish
  ```
- **All inline fee tables stripped** (FIX-16, FIX-17). §4 is now
  link-only across all 12 affected files. See policy below.
- **`research/fee-schedules/` directory deleted** along with every
  reference to it across STATE.yaml, synopses, RUNBOOK, PIPELINE,
  COVERAGE_STRATEGY, BACKLOG, specs/README, atlas.json schema.
- **`verdict` → `rating` across the entire codebase** (FIX-11). YAML
  field names, atlas.json keys, hook constants, atlas.js variables,
  CSS class names, data attributes — all migrated.
- **STATE.yaml** — `verdict:` → `rating:`, `verdict_basis:` →
  `rating_basis:`, `fee_research:` field removed entirely, header
  docstring updated.

### Atlas viz polish
- Map at the top (FIX-8), legend below the map (FIX-9), section
  headings reframed without "verdict" (FIX-9).
- Cards compact + sorted by rating color green→yellow→red→tbd→
  untracked (FIX-13).
- Per-card "Deep dive →" link, plus working drawer button (FIX-14).
- Local-dev URL rewrite: `docs.patentclient.com` → `localhost:8766`
  when atlas.html is served from `localhost`.

---

## §2 Policies established

### Fees — link only, never mirror
- Synopsis §4 describes **what categories** of fees an office charges
  (in local currency), lists **discount programs by name**, and links
  to the **official schedule URL**. That's it.
- **No inline amounts.** No percentages. No effective dates.
- The `research/fee-schedules/` directory was deleted entirely; we are
  not in the business of mirroring authoritative fee data — it drifts,
  the office's page is canonical at any moment, and reproducing it
  would lock us into a permanent re-research treadmill.
- The synopsis template (`research/templates/office-synopsis.md`) §4
  embeds the policy with `**Policy: link only. Reproducing fee
  amounts is *not our job*.**`
- The RUNBOOK §B is now an explicit "out of scope" notice.

### Substantive content drift discipline
- Synopses document **what information is available**, not the
  information itself.
- Citations to specific decisions (e.g., `CA/D 9/25`) in link labels
  are OK — they help readers navigate to the right notice. The
  substantive-data prohibition applies to inline numeric reproductions
  (amounts, percentages, effective dates) that would silently age out.
- Change log entries (§8) are historical decision records and stay; the
  hook collapses them to a "Last updated" footer in the published page.

### Vocabulary
- Public, user-facing word is **rating** (not "verdict" — litigation
  noun, out of place in IP).
- Color family: green, yellow, red, tbd, untracked. Each has a
  swatched legend entry.
- The hook keeps §-prefixes on renames so the rendered ToC reads
  continuously §1..§7. The verdict-callout anchor (now
  `#5-access-via-patent-client-agents`) updates in lockstep.

### Repo boundaries (unchanged — confirmed correct)
- `patent-client-agents` owns the **data** (research/, coverage/, mkdocs).
- `patentclient-web` owns the **interactive atlas** (D3 map, cards, drawer).
- jsDelivr CDN is the boundary between them. Both consume
  `coverage/atlas.json` and (legacy) `coverage/coverage.json`.

---

## §3 Architecture as it stands

```
research/STATE.yaml         ──┐  46 entities × {id, name, layer, iso2,
research/{layer}/*.md        ─┤  rights, rating, rating_basis,
coverage/sources.yaml        ─┤  connector_status, last_verified, ...}
                              ▼
                  scripts/build_coverage.py
                              │
                              ├──► coverage/coverage.json
                              │      • per-data-product view (40 sources)
                              │      • legacy /coverage page consumer
                              │
                              └──► coverage/atlas.json
                                     • per-office view (46 entities)
                                     • verdict + basis + synopsis_url
                                     • shipped_sources nested per office
                                     │
                                     │ jsDelivr CDN
                                     ▼
                       patentclient.com/atlas
                       ─ map colored by rating family
                       ─ 5-row legend strip
                       ─ compact 3-row filter bar
                       ─ card grid, sorted by color
                       ─ drawer with deep-link + GitHub fallback
                       ─ ?focus=ISO2 deep-link from docs side

docs_hooks/sync_patent_client_index.py
                              │
                              ▼
                docs.patentclient.com/patent-client-index/
                ─ §1 Mission · §2 What's unique here · §3 Programmatic surfaces
                ─ §4 Fees (link-only) · §5 Access via patent-client-agents
                ─ §6 Known unknowns · §7 References
                ─ Rating callout linked to atlas
```

---

## §4 Current freshness (audited 2026-05-18)

- **17 deep dives written**, all within 2 days of today
  (`2026-05-16` or `2026-05-17`). None stale.
- **29 entities without a synopsis** — split below.

Recent doc-policy refactors did **not** bump `last_verified`. That's
correct: `last_verified` tracks when we last checked the office's own
pages, not when we last reformatted our own writeup.

---

## §5 What's queued for the next session

See [the freshness audit at end of this session]
([`SESSION-2026-05-18.md`](SESSION-2026-05-18.md) — i.e. this doc).
The priority stack is:

### Priority 1 — shipped without a synopsis (3)
| Entity | Connector | Why it's a gap |
|---|---|---|
| `US/USCO` | shipped | atlas drawer falls back to GitHub research |
| `UPC/UPC` | shipped | UPC decisions + UPC statutes both shipped |
| `WO/WIPO/Lex` | shipped | substantive-law backbone |

Closing these is mostly a writeup job — the connector code already
encodes the operational knowledge.

### Priority 2 — rated, in-progress, no synopsis (4)
- `BR/INPI` (yellow_paid)
- `SG/IPOS` (red_no_api)
- `IL/ILPO` (red_no_api)
- `RU/Rospatent` (red_blocked)

### Priority 3 — rated red regional, no synopsis (4)
Quick stubs to lock in the verdict; not full deep dives.
- `OA/OAPI`
- `AP/ARIPO`
- `GCC/GCCPatent`
- `RU/EAPO`

### Priority 4 — unresearched secondary nationals (17)
Queue a discovery wave (grouped by region) when there's a reason to
expand. From [`research/RUNBOOK-research.md`](research/RUNBOOK-research.md) §A.

`AT/OPA · CH/IPI · NL/RVO · SE/PRV · FI/PRH · ES/OEPM · IT/UIBM ·
MX/IMPI · AR/INPI · NZ/IPONZ · PH/IPOPHL · ID/DGIP · TH/DIP ·
ZA/CIPC · SA/SAIP · AE/IPO · TR/TURKPATENT`

---

## §6 Open items / known issues

- **`patent-client-index` is a placeholder name** — we agreed to keep
  it until we find a better one. Replacement is one global string
  swap when the time comes.
- **Production docs not yet redeployed** —
  `docs.patentclient.com/patent-client-index/...` returns 404 in
  production. Atlas cards' `Deep dive →` links work locally
  (rewritten to `localhost:8766`) but will need a CF Pages
  redeploy + a fresh build of the new docs tree before they work in
  production.
- **Atlas reads `coverage/atlas.json` from jsDelivr**, which caches
  for a few minutes after push. Local dev fetches a local copy
  (`patentclient-web/atlas.json`) to avoid CDN lag.
- **The dev-browser `__name is not defined` page error** is an
  instrumentation artifact, not a real bug. Confirmed it fires on
  `about:blank` too. Does not affect real browsers.

---

## §7 Documentation produced this session

| File | What it is |
|---|---|
| [`ATLAS_INTEGRATION.md`](ATLAS_INTEGRATION.md) | The integration plan doc (phase-by-phase TODO; now mostly done) |
| [`SESSION-2026-05-18.md`](SESSION-2026-05-18.md) | **This doc** — session snapshot + handoff |
| `coverage/README.md` | Updated to document `atlas.json` alongside `coverage.json` |
| `research/README.md` | Cleaned references to the removed `fee-schedules/` dir |
| `research/RUNBOOK-research.md` | §B is now an out-of-scope notice |
| `research/PIPELINE.md` | Removed Step type B (fee research) from the state machine |
| `research/COVERAGE_STRATEGY.md` | §10 rewritten from "fee-schedule data architecture (future work)" to "Fee data — out of scope" |
| `research/BACKLOG.md` | Reconciliation log scrubbed of specific amounts |
| `research/templates/office-synopsis.md` | §4 prescribes link-only fee policy |
| `research/specs/README.md` | Dropped "Source fee research" line from spec template; renamed `Verdict basis` → `Rating basis` |
